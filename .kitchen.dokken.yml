---
driver:
  name: dokken

transport:
  name: dokken

provisioner:
  name: dokken
  always_update_cookbooks: true

verifier:
  name: inspec

platforms:
#  - name: ubuntu-16.04
  - name: centos-7.3

suites:
  - name: default
    run_list:
      - recipe[haproxy::default]
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
